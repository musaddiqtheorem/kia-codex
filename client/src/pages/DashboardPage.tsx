const DashboardPage = () => (
  <section>
    <h2>Dashboard (Scaffold)</h2>
    <p>
      This initial scaffold exposes core routes and API wiring points. Next step: add KPI cards,
      trend charts, and configurable widgets from the project specification.
    </p>
  </section>
);

export default DashboardPage;
